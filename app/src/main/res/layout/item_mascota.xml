<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:background="@drawable/bg_white_card"
    android:elevation="2dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="16dp">

    <!-- Avatar con inicial o foto -->
    <FrameLayout
        android:id="@+id/petAvatarFrame"
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:layout_marginEnd="14dp"
        android:background="@drawable/bg_avatar_amber"
        android:clipToOutline="true"
        android:outlineProvider="background">

        <TextView
            android:id="@+id/tvPetInitial"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="M"
            android:textColor="#C68A00"
            android:textSize="22sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/ivPetPhoto"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:visibility="gone"
            android:contentDescription="@null" />

    </FrameLayout>

    <!-- Información -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvItemPetName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Max"
                android:textColor="@color/text_primary"
                android:textSize="16sp"
                android:textStyle="bold"
                android:layout_marginEnd="8dp" />

            <TextView
                android:id="@+id/badgeActiva"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_active_badge"
                android:paddingHorizontal="10dp"
                android:paddingVertical="3dp"
                android:text="Activa"
                android:textColor="@color/white"
                android:textSize="11sp"
                android:textStyle="bold"
                android:visibility="gone" />

        </LinearLayout>

        <TextView
            android:id="@+id/tvItemPetInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:text="Labrador · 3 años · 28.0 kg"
            android:textColor="@color/text_secondary"
            android:textSize="13sp" />

    </LinearLayout>

    <!-- Editar -->
    <ImageView
        android:id="@+id/btnEditPet"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_marginEnd="2dp"
        android:padding="6dp"
        android:src="@drawable/ic_edit"
        android:tint="@color/blue_primary"
        android:contentDescription="Editar"
        android:clickable="true"
        android:focusable="true" />

    <!-- Eliminar -->
    <ImageView
        android:id="@+id/btnDeletePet"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:padding="6dp"
        android:src="@drawable/ic_trash"
        android:tint="#E74C3C"
        android:contentDescription="Eliminar"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
